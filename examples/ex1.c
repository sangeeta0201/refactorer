/*void bar(float *x){
}
void foo1(float *x){
//  *x = sqrt(*x) ; //there should be error here and it should tace back to float xx
  bar(x);
}
*/
/*
void bar2(float *x){
    *x = (*x) * (*x); //there should be error here and it should tace back to float xx
      printf("%e\n", *x);  //(xx+x)*(xx+x)
}
float cordic_sin(float theta){
}
*/
double foo(int n){
 // float z,di;
  double R,Q,CR2,CQ3,a,b,sgnR, R2, Q3;
//  double *x0, *x1;
  /*
  if (R == 0 && Q == 0)
  {
    *x0 = - a / 3 ;
  }
  else if (CR2 == CQ3)
  {
    *x1 = - a / 3 ;
  }*/
  double A = -sgnR * pow (fabs (R) + sqrt (R2 - Q3), 1.0/3.0);
 // float theta, sin;
//  theta = -sin;
//  double sgnR = (R >= 0 ? 1 : -1);
//  double q = (a * a - 3 * b);
//   *x0 = - a / 3 ;
//  for (float theta = sin; theta < 1.54; theta = theta + 0.00000065) {
//    cos += theta;
//  }
 // if (fabsf(diffX) != 0.0) {
//    cordic_sin(theta);
//  }
/*
  if (z >= 0) di = 1.0;
          else 
            di = -1.0;
*/
}

