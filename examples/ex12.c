#define LOCAL( grid, C  ) A[0][0]
#define DFL1 +2.3*4.3
#define N_CELL_ENTRIES 10
int foo(){
	int n = 10;
	int m = 10;
	int i = 2;
	int j;
	double z = sqrt(2.3);
	double minU2  = sqrt(1e+30), maxU2  = -1e+30, ux, rho = 3.4;

	printf("%e", sqrt(minU2));
}
