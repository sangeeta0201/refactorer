int plus1mod3[3] = {1, 2, 0};
struct behavior {
	double minangle, goodangle, offconstant;
	double maxarea;
};

double bar(double x){
	return x;
}
#define PI 3.141592653589793238462643383279502884197169399375105820974944592308
int foo(struct behavior *b){
	double x = 2.3;
	double y = 2.4;

	//double k ;
	bar(2.3); 

	double z = x*bar(x*y); 
	sqrt(180.0);

	cos(PI / 180.0);


	double goodangle = cos(PI / 180.0);
	double g1; 
	g1 = cos(PI / 180.0);

	cos(180.0);
	double goodangle2 = cos(180.0);
	double goodangle3;
	goodangle3 = cos(180.0);

	printf("%e", goodangle);
	if(b->offconstant == 1.0)
	b->offconstant = 0.475 * sqrt((1.0 + b->goodangle) / (1.0 - b->goodangle));

}
