#define LOCAL( grid, C  ) A[0][0]
#define DFL1 +2.3*4.3
#define N_CELL_ENTRIES 10
	posit32_t epsilon;posit32_t nflavors;
int foo(posit32_t  z, posit32_t  zz){
	int step = 0;
}

#include "softposit.h"
main(){
	posit32_t tmp1=convertDoubleToP32 (5.0E-1);
	posit32_t tmp2=convertDoubleToP32 (8.0E+0);
	posit32_t tmp3 = p32_mul(tmp1,tmp2);
	posit32_t tmp4=convertDoubleToP32 (8.0E+0);
	posit32_t tmp5 = p32_div(nflavors,tmp4);
	posit32_t tmp6=convertDoubleToP32 (5.0E-1);
	posit32_t tmp7 = p32_sub(tmp6,tmp5);
	posit32_t tmp8 = p32_add(tmp3,epsilon);
	posit32_t tmp9 = p32_mul(epsilon,tmp7);
	foo(tmp9, tmp8);
}
